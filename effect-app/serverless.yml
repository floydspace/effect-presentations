service: demo-effect-app
frameworkVersion: "3"

plugins:
  - serverless-esbuild
  - ./provided-runtime-plugin.js

provider:
  name: aws
  runtime: nodejs20.x
  region: eu-west-1
  iam:
    role:
      statements:
        - Effect: Allow
          Action: sns:Publish
          Resource: !Ref DomainTopic

custom:
  esbuild:
    minify: false
    metafile: true
    exclude: []
    mainFields: ["module", "main"]
    alias:
      mongodb: mongodb/src
    packager: pnpm

functions:
  lambda:
    handler: src/bun.fetch
    layers:
      - arn:aws:lambda:eu-west-1:780689416838:layer:bun:1
      - arn:aws:lambda:eu-west-1:580247275435:layer:LambdaInsightsExtension:53 # Needed for graceful shutdown
    environment:
      MONGODB_URL: mongodb+srv://cluster0.tlkgc.mongodb.net/demo?retryWrites=true&w=majority&authSource=%24external&authMechanism=MONGODB-AWS
      DOMAIN_TOPIC_ARN: !Ref DomainTopic

resources:
  Resources:
    DomainTopic:
      Type: AWS::SNS::Topic
      Properties:
        TopicName: ${self:service}-domain-topic
